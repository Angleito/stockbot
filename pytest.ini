[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not integration and not finra_smoke"
markers =
    integration: tests that call live SEC/OpenRouter services
    finra_smoke: opt-in live FINRA mock-endpoint smoke test (RUN_FINRA_SMOKE=1 + credentials)
